# Implementation task briefs

Prepared 5 September 2026. Review and task preparation are complete; implementation progress and outstanding requirements are recorded below. Models below are recommendations, not instructions to change models or delegate.

## How to run

Open each task in the existing Church Tracker project and run sequentially. Select the model and reasoning setting before sending the launcher prompt. Continue the same task for corrections. Start the next task after the previous handoff is ready.

The current saved project contains substantial uncommitted work. A fresh worktree from the default branch may omit that work and these new briefs. For this sequential programme, use the saved project directly. An agent can establish a feature branch while preserving current files. Keep completed work and task documents available to later tasks; do not silently start again from an older commit.

Read this short index and the selected brief. Each brief contains the objective, useful starting points, and completion checks. The earlier review is evidence to consult as needed, not a requirement to re-audit every file.

## Order and status

| Task | Brief | Suggested model / reasoning | Status |
|---|---|---|---|
| 01 | 01-environments.md | Terra / High | Complete — isolated staging verified |
| 02 | 02-security.md | Astra / High | Complete — security and HTTPS staging sessions verified |
| 03 | 03-people.md | Terra / Medium | Complete — record integrity verified |
| 04 | 04-attendance-care.md | Astra / High | Complete — local transactions and browser workflows verified |
| 05 | 05-reporting.md | Terra / Medium | Complete |
| 06 | 06-backups-uploads.md | Terra / High | Complete — isolated file-inclusive recovery rehearsal verified |
| 07 | 07-ui-documentation.md | Luna / Medium | Implemented locally — final 390px browser verification remains |
| 08 | 08-operations-review.md | Terra / High; final independent review with Astra / High | Implemented locally — non-production deployment/subscription verification remains |
| 09 | Google sign-in and access finishing | Current task | Implemented locally — church OAuth configuration and isolated staging verification remain |
| 11 | Integrated completion and staging release | Current task | In progress — backend and Preview deployed to isolated staging; authenticated two-client evidence and provider decisions remain |

These split the five earlier broad batches into smaller assignments. Task 07 is deliberately bounded for Luna. For Task 08, use Terra to implement; a subsequent Astra review can use the same brief's final-review section. Do not start that review automatically or create another task without the user's request.

## Handoffs

Append one concise entry per task: status/date, changed behavior, validation results, remaining work or required user input, and current branch/commit or uncommitted state. Keep credentials and private records out of this file. If external configuration prevents verification, state that and keep the task incomplete instead of using mock success as proof.

### GitHub and production reconciliation — 9 September 2026

- Audited the integrated workspace against GitHub and Vercel before release. GitHub `main` remains at `f7b9f2a`; the release branch remains at `9abda19` before this reconciliation, while the source/docs/CI/backup work below was local only. The existing Vercel production deployment was a direct upload (no Git source metadata) and served the integrated application, but could not be traced to a commit.
- Prepared the complete release source set: frontend, Convex schema/functions/tests, deployment configuration, recovery/implementation handoffs and the private-output-safe backup script. GitHub rejected the new CI workflow because the authenticated OAuth token lacks its separate `workflow` scope, so `.github/workflows/verify.yml` remains preserved locally for a scope-authorized follow-up. Generated coverage/Playwright results and the local `AGENTS.md` instructions also remain outside the commit; `.gitignore` now excludes the generated result directories.
- Checks: frontend unit tests (183), backend tests (57), backend TypeScript, production build and `git diff --check` pass after repairing three local type-safety issues in the reviewed import/development code. Browser verification confirmed the production alias loads the integrated dashboard and current section navigation. A matching production frontend deployment and push are recorded with the release commit.
- State: `codex/staging-accounts-and-verification`; this handoff is committed with the release reconciliation. No source import, reset, backup deletion or church-record write was performed.

### Church settings and reviewed import preparation — 9 September 2026

- Reviewed current church-profile placeholders, environment selections and all four source-sheet tabs. Recorded the user-confirmed church/address and ACTS/Jayden scope; no settings or records changed. [Import review handoff](church-import-review.md) records findings and remaining requirements; private source rows and proposed mapping are stored outside Git.
- Source totals reconcile to 130 recorded attendances including seven first-timer visits, with explicit absence and unknown cells preserved separately. Identity decisions, early-departure evidence, shared collectors, missing surnames/birth years, and first-contact/first-visit meaning remain held for review/model support.
- The shared destination is still unconfirmed. Explain it as the practice copy versus shared church version; current frontend/CLI defaults point to different non-staging environments. Shared launch, current-record comparison, reviewed repeat-safe writes and regular file-inclusive backups remain required.
- Checks: read-only connector/code/config inspection and reconciliation calculations. No browsing, tests or build, per user request. No database read/write, import, deployment, source-sheet edit, or backup schedule change.
- State: `codex/staging-accounts-and-verification`, base `9abda19`; inherited uncommitted work preserved, with documentation/private review artifacts added. No commit/reset/stash.

### Task 01 — complete (5 September 2026)

- Added one validated Convex environment configuration shared by all frontend services; `VITE_APP_MODE=demo` is allowed only with `VITE_APP_ENV=development`, and live mode without a URL is an explicit unavailable state. Updated the People directory so an empty live database remains empty and people without coordinates never gain sample people. CRM and pastoral-care loading no longer silently switch to the local workspace on live failures. Demo persistence/reset and development, staging, preview, and release workflow are documented in `.agent/workflows/environments.md`.
- Checks passed: `npm run test:unit` (132 tests), `npm run build` (existing accessibility/Caniuse warnings only), browser checks of `/people` with no URL (visible configuration error, zero people), explicit demo mode (visible demo banner and 30 demo people), and a live Google/Auth0 sign-in plus empty-data dashboard check against isolated staging.
- External setup completed: Convex project `Church-tracker` development URL is configured for local/Vercel Development; a separate `church-tracker-staging` project was created with deployment URL `hidden-guineapig-648.convex.cloud`; Vercel Preview points to that staging URL, while Vercel Production points to the existing `elated-bee-284.convex.cloud`; `VITE_APP_ENV` and `VITE_APP_MODE=live` are scoped for Development, Preview, and Production. The current Convex functions were deployed to development and isolated staging only. A first owner access record was provisioned in each non-production deployment through the documented maintenance bootstrap, then the maintenance flag was removed. Auth0 now has the Church Tracker SPA with Google sign-in plus local and known Vercel production callback/origin URLs. No redeploy was triggered.
- Acceptance browser check passed against isolated staging: Google/Auth0 sign-in completed, the protected dashboard loaded from `hidden-guineapig-648.convex.cloud`, and the empty church database remained empty (zero members, services, follow-ups, pastoral-care items, and recent activity). The existing development deployment's older records were not reset or modified. No seed/reset or church-record write was performed.
- Production remains untouched: no production functions were deployed, no production data was written, and no Vercel deployment was triggered. `.env.local` was restored to the existing development deployment after staging verification.
- State: `main` at `f7b9f2a`, with substantial inherited uncommitted work preserved plus this task's uncommitted changes.

### Task 05 — Complete (5 September 2026)

Shared reporting definitions now exclude future/scheduled gatherings from actual attendance, distinguish held meetings, prayer-only duration, successful care, and inclusive calendar-date boundaries; Reports and meeting analytics use them, dashboard guest terminology now says “Guest attendances,” and CSV exports neutralize formula-like text while preserving phone values as text. Focused reporting fixtures pass (held meeting average, repeat first-timer deduplication, prayer/care classification, boundaries, CSV safety); `npm run test:unit` now passes (136 tests); `npm run build` passes; local browser verification covered Reports overview, Services, Meetings, and Pastoral Care labels/counts. The earlier missing-Convex full-suite blocker was resolved by the sequential Task 02 integration; no backend or production action was performed for Task 05. Branch `main`, base `f7b9f2a`; all work remains uncommitted alongside inherited changes.

### Task 05 reports follow-up — complete locally (9 September 2026)

- Refreshed Reports to match the current section navigation, label period-scoped and all-time figures explicitly, avoid showing demo records while live data loads, and surface failed multi-source refreshes with a retry instead of silently reporting zeroes. Meeting report totals now use the same derived named-plus-unnamed attendance value as the Meetings workspace; overview totals and detail calculations share tested helpers.
- Checks: focused reporting tests pass (5); production build passes; built-in browser verified overview/detail navigation and a 390px viewport with no horizontal overflow. The complete unit run reached 171 passing tests but remains blocked by four inherited `WeeklyAttendanceMatrix.test.js` failures (`serviceColumnWidth()` is called as a function after its current change to a derived value), outside this reports follow-up.
- State: branch `codex/staging-accounts-and-verification`; changes remain uncommitted alongside the substantial inherited working tree. No backend, deployment, account, export, or church-record write was performed.

### Task 02 — complete (5 September 2026)

- Implemented approved Google/Auth0 sign-in, session-derived profile/sign-out, authenticated client requests, and backend owner/admin/assigned-leader/summary-viewer boundaries. Confidential care requires an explicit grant; writes attribute the authenticated account. CSV export requires administrator access; mock backup is disabled pending Task 06. Maintenance is internal and disposable reset covers the schema. Map popup fields render safely as text. Follow-up fixes keep sign-out across reload/provider failure, propagate it between tabs, and prevent old rejected requests from revoking a newer session.
- Checks pass: **136 frontend/unit tests, 15 backend tests**, coverage, backend TypeScript, official Convex code generation/type checking, and build. Backend tests cover every public handler, roles, assignment scope, confidential access, forged actors, account/assignment revocation and maintenance. Built-in browser popup safety checks remain valid. Full audit: **0 critical/high/moderate**, four low entries for one specifically documented SvelteKit cookie advisory; production-dependency-only audit: zero.
- Live isolated-staging checks pass: real Google owner sign-in and actual profile; two authenticated tabs clear protected pages on sign-out; reloading `/people` stays signed out; anonymous/malformed/forged-signature requests fail; seed/reset/migration/bootstrap calls are inaccessible to ordinary clients, including an authenticated owner. Operator-simulated unapproved identity is denied by the deployed approval handler. The HTTPS staging preview also passes automatic restoration after a full reload and in a second tab, forced Auth0 renewal with backend acceptance of the renewed token, and two-tab sign-out. Staging retains zero people and one active owner; no records or production deployments were changed in these follow-up checks.
- Created one non-production [HTTPS preview](https://my-church-tracker-9pq0sojip-jaydenayeh-1940s-projects.vercel.app), connected to isolated staging, and added only its exact Auth0 callback/logout/web origin while preserving existing entries. Production aliases and deployments were not promoted or changed. Local loopback callbacks still require Google confirmation on reload; the app explains this and protects personal content. Before release, replace the shared Google developer credentials with church-owned OAuth credentials and recheck the final origin/browser configuration. Exact acceptance evidence, setup and dependency disposition: [Task 02 security handoff](task-02-security-handoff.md).
- State: `main` at `f7b9f2a`, all changes uncommitted alongside preserved inherited work. Tasks 03–06 remain paused for sequential resumption. No production release was made.

### Task 03 — complete (5 September 2026)

- Aligned person form, service payload, Convex validators, and schema for city, state, and postcode. Updates now use explicit `clear_fields`, so blank optional values remove the saved field while omitted values remain unchanged; canonical writes convert legacy `visitor` to `guest`. Evangelism uses the same server-side field/date/email validation.
- Added normalized email/phone duplicate warnings (never name-only), an explicit “save separately” acknowledgement, safe archiving, and permanent-delete refusal when any person history exists. The profile now has a reviewed merge dialog: it shows linked-record counts, refuses ambiguous or account-linked cases, rechecks stale previews, moves conflict-free relationships, and archives the source duplicate.
- Checks passed: `npm run test:backend` (**17 tests**), `npm run check:backend`, `npm run test:unit` (**136 tests**), and `npm run build`. The authenticated in-memory Convex regression covers full-address create/edit/clear/reload, invalid validation, predictable duplicate matching, archive/delete protection, and reviewed attendance-preserving merge. Built-in browser check confirms the unsigned local `/people` route exposes only the approved-account sign-in screen; no account or church records were entered.
- State: `main` at `f7b9f2a`, with Task 03 and inherited work uncommitted. No production deployment or live database write was made. Existing unrelated build accessibility warnings remain outside this brief.

### Task 06 — complete (5 September 2026)

- Replaced the browser-only mock backup with an owner/admin-only link to Convex Backup & Restore; the UI no longer claims a summary JSON can recover church data. Added [recovery documentation](../recovery.md) covering provider retention defaults, file-inclusive snapshots, isolated import/restore rehearsal, access checks, and destructive-restore safeguards. CSV reports remain separate.
- Added durable `service_photos` records tied to Convex `_storage`, owner/admin-gated short-lived upload URLs, server-side JPEG/PNG/WebP and 10 MB validation, progress/failure feedback, pending-upload cleanup, attachment/removal on service save, and file deletion when a service is deleted. Browser object URLs are previews only; reloaded services resolve their image URLs from the backend references.
- Checks pass: `npm run check:backend`; `npm run test:backend` (**38 tests**); `npm run test:unit` (**141 tests**); and `npm run build`. Existing unrelated build accessibility warnings remain. The photo picker is now keyboard/assistive-technology reachable, and its asynchronous cleanup retains the original input reference.
- Isolated provider rehearsal completed without production or shared-staging changes. In `church-tracker-recovery-rehearsal` / `judicious-firefly-551`, an approved owner created two clearly labelled synthetic people, one service, two attendance links, and one PNG service photo. A CLI snapshot export with `--include-file-storage` restored cleanly into empty `church-tracker-recovery-restore` / `tough-chinchilla-369`: **2 people, 1 service, 2 attendance links, 1 `service_photos` record, and 1 stored file**. The restored owner signed in, the original relationship IDs/links were preserved, and the restored Services view reloaded and rendered the photo. Backend regression asserts anonymous, viewer and leader callers cannot generate upload URLs; the live owner upload succeeded.
- The isolated source deployment has no dashboard backup yet and automatic backups are disabled (shown as a Pro feature). On 5 September 2026, an operator-requested manual, file-inclusive production backup was completed for `elated-bee-284`; it expires in seven days. No automatic schedule or paid-plan upgrade was enabled. The recovery guide records Convex's documented seven-day manual/daily and fourteen-day weekly defaults, and requires confirmation of the live plan, schedule, file inclusion, access roster, and retention obligations before operational reliance. The two labelled, isolated rehearsal environments and their synthetic records are retained for review; delete them only when explicitly approved as disposable. No production deployment, production data write, or production restore was performed. State: `main` at `f7b9f2a`, with all Task 06 and inherited work uncommitted.

### Task 04 — complete locally (5 September 2026)

- Unified named service/meeting attendance, CRM actual-attendance resolution, member plans and first-visit/promise milestones through shared atomic backend rules. Added explicit gathering selection for ambiguous dates, retry IDs on recording forms, valid named/unnamed totals, backdated corrections, future-attendance refusal and legacy promise-write retirement. Meeting details/check-ins now save together.
- Care create/edit/removal maintains one linked follow-up timeline. Corrections preserve completed source/next tasks and never create replacement completed work. Deletion snapshots support operator-assisted recovery while attendance/plan/history cleanup and task cancellation follow explicit policies. Do-not-contact/pause checks and Task 02 administrator/confidential-care boundaries are retained.
- Final checks passed: `npm run test:unit` (**141 tests**), `npm run test:backend` (**38 tests**, including **20 Task 04 transaction regressions**), `npm run check:backend`, official Convex code generation/type checking, and `npm run build`. Built-in browser checks used the actual service, meeting, care and CRM attendance components with authenticated in-memory Convex transactions: ambiguous gathering selection, shared check-in history, service-date corrections, earlier meeting first visits, and care corrections without reopening a completed task. Existing unrelated build/whitespace warnings were preserved.
- Remaining requirements: apply/smoke-test schema/functions in non-production before frontend release; review ambiguous/unlinked historical care individually; Task 06 owns complete backups and restore tooling. Recovery snapshots have no public restore API. No Task 04 deployment or live church-record write was performed. Detailed policies, evidence and a reproducible browser fixture: [Task 04 handoff](task-04-attendance-care-handoff.md).
- State: `main` at `f7b9f2a`; all Task 04 and inherited work remain uncommitted. Concurrent Task 05/06 handoff/storage/test updates observed during final verification were preserved.

### Task 07 — implemented locally (5 September 2026)

- Fixed the Contact Drawer accessibility warnings and gave it modal behavior: focus moves into the drawer, Tab is contained, Escape/backdrop close it, and focus returns to the opener. Profile/status menu warnings are resolved, table sort/column/individual-attendance actions have accessible names, and the stale Sidebar selector was removed. Form dialogs now use explicit Cancel actions with unsaved-change confirmation while save failures leave entered values in place.
- Kept phone task flow prominent in Meetings with the existing Quick record and attendance-needed sections; demo browser verification showed these actions and the authenticated demo empty/loading conventions. Live notifications and global search no longer show invented people, church details, health figures, or seeded alerts. Demo examples remain available only when `VITE_APP_MODE=demo`; live notifications show an honest no-feed state. A real notification source and live search index remain Task 08 backend requirements.
- Refreshed the root README and documentation hub with the validated development, explicit demo, preview, release, test, backend-check, and provider-managed recovery behavior. No production deployment or church-record write was performed.
- Checks passed: `npm run test:unit` (**141 tests**) and `npm run build`. The scoped Svelte accessibility warnings are gone; only existing Vite chunk-placement notices remain. Built-in browser checks in explicit demo mode covered Meetings quick record, the profile-menu Escape/focus return, accessible table actions, and the People form without submitting data. Native discard confirmation was opened during the People check, but browser automation timed out while the native dialog was appearing; no data changed. Exact 390px viewport emulation was unavailable in the current built-in browser surface, so final release should repeat the keyboard/discard check at 390px.
- State: `main` at `f7b9f2a`, with Task 07 and all inherited work uncommitted and preserved. No additional account choice or deployment is required for this local task.

### Task 08 — implemented locally; not production-ready (5 September 2026)

- Dashboard reads no longer call a mutation. Quarterly re-engagement and paused-contact restart now share one idempotent routine, run daily at 00:10 UTC as an internal Convex cron, and retain the administrator-only backfill endpoint. CRM reads now use status/date/member-status indexes for the main operational slices instead of whole-table reads; the deterministic 100-contact dashboard fixture completed within the enforced 1.5-second test budget without creating work.
- Active Follow-Up pages now subscribe to Convex dashboard updates and clean up subscriptions on filter change/unmount, replacing the five-minute poll. Live notifications are derived from authenticated saved CRM work, and global search uses scoped Convex name search indexes; neither has a live-mode sample-data fallback. CI now runs frontend/backend tests, backend type checks, coverage, and the production build. [Release runbook](../release.md) documents staged frontend/backend releases, indexes/migrations, backups, and rollback limits.
- Checks passed: `npm run test:unit` (141), `npm run test:coverage`, `npm run test:backend` (42, including Task 08 idempotency, alert, search, and load-fixture checks), `npm run check:backend`, and `npm run build`. Built-in-browser demo verification loaded Follow-Up and returned the expected demo global-search result; no forms were submitted and no records changed.
- Remaining acceptance evidence: deploy the schema/indexes/functions to an explicitly selected **non-production** deployment, wait for the indexes to build, and verify with two approved browser clients that one client’s CRM change reaches the other via the subscription. Confirm the scheduled run after it fires and that the live notification/search queries work against that same isolated deployment. This task intentionally made no deployment or church-record write; therefore it is not production-ready. The optional independent Astra review is not started.
- State: `main` at `f7b9f2a`, with all Task 08 and inherited changes uncommitted and preserved. A local `convex codegen` attempt was rejected by Convex before upload because of a duplicate temporary index; that index was removed, and no backend deployment succeeded.

### Integrated completion check — 5 September 2026

- Fresh verification of the combined working tree passed: 141 frontend tests, 42 backend tests, backend TypeScript, and the production build. Coverage was not rerun in this review. All implementation remains uncommitted on `main` at `f7b9f2a`; preserve the inherited changes. No release or live record write was performed.
- Task 07 remains open: the built-in browser now supports exact viewport testing. At 390 × 844, local demo `/people` had a measured document width of 424px and the top-right profile control was partly clipped. Fix the mobile title/action flex layout in `src/lib/components/layout/TopNav.svelte` (around lines 241–260), then repeat the mobile checks. The person form opened and focused First Name; Cancel with an edited first name triggered the expected discard confirmation, but browser dialog control timed out, so input preservation and focus return after that confirmation remain unverified.
- Task 07/08 integration cleanup: `NotificationDropdown.svelte` still always says “No live feed connected” in live mode and its empty message says a source needs connecting, although Task 08 implemented `notifications.getFeed`. Update the wording to reflect loading, successful empty results, and unavailable requests; the current refresh silently retains cached results on failure.
- Task 08 remains open: the CI workflow runs unit/backend/type/coverage/build checks but has no automated browser end-to-end step. Close or explicitly defer the brief's critical end-to-end CI requirement. The recorded same-staging deployment, index build, two-client update, live search/notification, and actual scheduled-run checks are still required; passing in-memory tests does not provide this evidence.
- Before real-data release, also close the existing church-owned Google OAuth credential requirement and confirm an ongoing file-inclusive backup schedule/retention/access policy. The recorded one-off seven-day backup does not establish ongoing protection. Continue with one focused finishing pass rather than repeating all eight implementation tasks.

### Task 09 — implemented locally; external setup and verification remain (5 September 2026)

- Diagnosed and fixed a local sign-in dead end: the Auth0 SDK's unbounded startup `checkSession()` could leave the app on “Checking your session…” with no Google button. Session restoration remains available, but it now has a four-second bound and then exposes explicit Google sign-in. The built-in browser verified the local screen advances to “Sign in to your church”; no personal Google account was used during this check.
- Added a request-and-approval access flow. A signed-in, provider-verified but unapproved identity can request owner review; it gains no access automatically. Owners use `/access` (from Profile → Manage access) to approve a pending request, choose an existing role, link leaders to a person, grant confidential access separately, and activate/deactivate approved accounts. The normal UI neither displays nor edits issuer/subject identifiers; provider email is explicitly a recognition hint, not identity proof. Backend mutations preserve owner-only control, exact token-identifier binding, last-active-owner protection and session-attributed audit entries.
- Checks passed: `npm run test:backend` (42 tests, including approval, role/confidentiality, revocation and last-owner cases), `npm run check:backend`, `npm run test:unit` (141 tests), `npm run build`, targeted diff whitespace check, and local built-in-browser sign-in-screen verification. Existing unrelated whitespace warnings in `src/app.css` and `AttendanceTrend.svelte` were preserved.
- Remaining external requirements: deploy these schema/functions and frontend only to an explicitly selected non-production deployment, then verify an approved owner and a separate Google account can submit, approve, use and revoke access. The church owner must replace/confirm the Auth0 Google connection uses church-owned OAuth credentials and retain only the exact approved callback/logout/web origins in the provider UI; credentials must not be placed in this repository or chat. Production was not deployed or changed. If the original click still shows an error after this local fix, record the exact message and browser address for follow-up.
- State: `main` at `f7b9f2a`, with all Task 09 changes and substantial inherited work uncommitted; no data reset, bootstrap, account approval, provider change or production action was performed.

### Task 10 — incomplete locally (5 September 2026)

- Tightened `TopNav.svelte` mobile flex sizing: the title can now shrink/truncate while the search, notifications, and profile actions remain visible. `NotificationDropdown.svelte` and `notificationStore.js` now expose honest live-feed loading, successful-empty, and unavailable states; unavailable refreshes clear stale live alerts instead of silently retaining them. No backend changes were required; Task 08's `notifications.getFeed` remains the source.
- Checks passed: `npm run test:unit` (**141 tests**), `npm run build` (existing Vite chunk-placement notice only), and built-in browser verification at **390 × 844**. The `/people` document measured **382px** wide, the profile control ended at **374px**, and Profile → Escape returned focus to the profile opener. Demo notification behavior remained intact.
- Remaining acceptance check: editing First Name and pressing Cancel opens the native discard confirmation, but the browser automation timed out while the native dialog appeared; its dismiss/accept result and post-confirmation input/focus behavior remain unverified. No person was saved or changed. Live-feed state wording was code/build verified but could not be exercised against an authenticated live account in this local check.
- State: `main` at `f7b9f2a`, with Task 10 and all inherited work uncommitted and preserved. No deployment, account change, data reset, or church-record write was performed.

### Task 11 — staging release and integrated completion (6 September 2026; incomplete)

- Added a Playwright browser workflow to CI. It runs the existing authenticated in-memory Convex fixture and proves a user must select one of two same-day gatherings, records the resulting attendance correction, creates linked care work, and completes that work without reopening it. CI now installs Chromium and runs `npm run test:e2e`; the fixture supplies the production CRM module's subscription shape without attaching a persistent test listener.
- Full local verification passed: `npm run test:unit` (141 tests), `npm run test:backend` (42 tests), `npm run check:backend`, `npm run test:coverage`, `npm run test:e2e` (one critical workflow), and `npm run build`. Built-in-browser verification at 390 × 844 measured a 382px document width, with the profile action ending at 374px; Profile → Escape returns focus to its opener. The native person-form discard prompt appears, but this browser surface cannot accept/dismiss that OS-level prompt, so its post-confirmation input/focus branch remains an explicit small check.
- Verified the named isolated staging deployment is `church-tracker-staging` development deployment `hidden-guineapig-648`, not the project's unrelated current default `tangible-jackal-366`. It contained zero people/services and one pre-existing approved owner. Created a fresh file-inclusive staging snapshot in restricted temporary storage, then deployed schema/functions to `hidden-guineapig-648`; required access, CRM, notification, service-photo functions and new indexes reported ready. Local development configuration was restored afterwards. Released the matching Vercel **Preview** frontend at https://my-church-tracker-hjyvy0wnx-jaydenayeh-1940s-projects.vercel.app; its unauthenticated `/people` screen reaches “Sign in to your church.” No production deployment, production data write, reset, migration/import, provider permission change, or staging church-record write occurred.
- Authenticated staging verification now passes with the church owner's Google account: the Preview callback/logout/origin was added to the Auth0 development application, the owner dashboard loaded, and a second Google account requested then received a temporary linked **leader** role. That account can sign in and use the dashboard, while its profile does not expose owner-only access management and its recovery console is disabled. The request/approval and role-boundary path is therefore verified against the deployed staging stack, not a mock.
- Test note: the browser delayed its person-create confirmation and created two clearly labelled `Staging Test Leader` guest records; one is linked to the temporary leader account. These are the only staging church records introduced during this verification and should be cleaned up with the temporary account after the remaining workflow checks.
- Remaining acceptance requirements: perform a real person change, attendance correction, follow-up completion, durable photo reload, two-client CRM propagation, and live notification/search checks. Observe the deployed 00:10 UTC scheduled job after it actually fires and record that separately from any manual test. The church must confirm its Google/Auth0 connection uses church-owned OAuth credentials with only approved origins, and choose/confirm an ongoing file-inclusive backup schedule, retention and access roster; the one-off snapshot does not establish this and a paid Convex backup upgrade remains a user decision. Production release and the optional final Astra security review remain separate decisions.
- State: `main` at `f7b9f2a`, with all Task 11 and inherited work uncommitted and preserved.

### People UI review and refinement — 8 September 2026

- Follow-up requested for the People directory/map/profile, independent of the numbered release programme. Removed the unused date filter, duplicate quick-view cards, arbitrary profile scores/radar, and map controls dependent on demo queues or a sample church origin. Added a searchable location panel, missing-pin coverage, safe profile popups, a shared map route, compact contact header, activity/care and background-detail views, factual attendance summary, and readable paginated attendance. Notes now use the saved person form; fixed that form's snapshot effect loop and made history failures visibly unavailable.
- Checks: 150 unit tests and production build pass. Built-in browser covered desktop/390px map and profile layouts, search/empty state, pin focus/profile navigation and notes save in explicit local demo with contacts retained. Both map/profile measured 382px document width at a 390px viewport. No backend change or deployment; live authenticated verification is separate.
- Product boundary: no geocoding/address-to-pin workflow or replacement route planner was added; those require a defined location/care workflow. Full findings and rationale: [People UI review](../critiques/people-ui-review-2026-09-08.md).
- State: `codex/staging-accounts-and-verification` at `9abda19`, all changes uncommitted. Inherited untracked `.github/`, `AGENTS.md`, `coverage/`, and `test-results/` preserved.


### Participation radar and leader review — 8 September 2026

- User clarified that the radar should remain and combine participation with a leader assessment, including members growing toward leadership. Added Participation & discipleship to every profile (expanded for members/workers), with 12/24 completed weeks of Sunday/prayer/bacenta/worker attendance, selectable evidence, dated inviter-linked contacts, explicit tithe dates and school milestones. Removed competitive scoring; missing/inaccessible history remains unavailable.
- Added dated leader reviews with journey focus, understanding, agreed next step, optional next review and retained history. A new optional people field and Convex append mutation enforce existing confidential/person access, server-authored attribution and validation without changing membership or account roles. Failed saves keep the draft.
- Checks: 158 frontend tests, 46 backend tests, backend TypeScript, production build and diff whitespace check pass. Built-in browser verified radar selection/period, review save/reopening and desktop/390px editor in explicit local demo; document width 382px at 390px. In-memory Convex tests cover persistence, assignment/confidential restrictions, forged authors and revoked access.
- Remaining live requirement: deploy matching schema/functions/frontend to the selected staging environment and verify authenticated saving there. No deployment or live record write was performed. Existing gathering permissions can leave assigned leaders’ attendance unavailable; a future scoped summary query should be designed before granting broader gathering access. Inviter links do not track repeat invitations; current tithe flags cannot prove regularity without dated records.
- State: `codex/staging-accounts-and-verification` at `9abda19`; changes remain uncommitted, inherited work preserved. Full rationale in [People UI review](../critiques/people-ui-review-2026-09-08.md).

### Dashboard and analytics cleanup — 8 September 2026

- Follow-up to browser comments across Dashboard, Evangelism, Follow-Up, Sunday Services and Meetings. Standardised dark native dropdown menus and enlarged chart comparison controls; reserved space for fullscreen actions so legends no longer collide. Evangelism now uses “First-time attendees”, provides fullscreen outreach trends, and opens named-person outcome details. Follow-Up worker selection now returns to and filters the list, excludes unassigned people from a named worker view, visibly animates Refresh, and separates View list from activity Details.
- Sunday Services now presents tithers as a separate inner ring because they are a subset of member attendances; member, guest and tither rings can each be hidden and restored. The redundant Service mix card is omitted when the period contains only one service type. The person matrix now shows up to 16 recent Sundays from the available history and has fullscreen mode. Meetings now leads with an adjustable programme comparison (average, total, unique people or meetings held), replaces the ambiguous journey graph with explained person groups, and removes the confusing roster-opportunity chart.
- Checks passed: `npm run test:unit` (158 tests), `npm run build`, and `git diff --check`. Built-in browser verification covered the Evangelism outcome modal and fullscreen trend, the three Services ring toggles, conditional Service mix removal, the 16-Sunday matrix, the four-metric Meetings comparison and explained person groups, the Follow-Up worker filter/list switch, worker View list/Details actions, and the uncluttered Dashboard chart header. No backend, deployment or church-record write was performed.
- State: `codex/staging-accounts-and-verification` at `9abda19`; this cleanup and substantial inherited work remain uncommitted and preserved.

### Dashboard interaction refinement — 8 September 2026

- Follow-up to the second browser review. The home attendance card now uses the same shared chart language as Services, with the project-styled searchable comparison control. Services can compare attendance with tithers, animates all three attendance-ring toggles, opens service-by-service decision/tither breakdowns, and lets the weekly matrix add or remove Sunday columns. The record-service modal now opens on the setup step without fetching the people directory; that work begins only when attendance is requested.
- Follow-Up list rows now open the person detail while preserving their checkbox, communication and assignment controls. Top navigation has labelled Back and Forward history controls. Continuous pulses on the unread badge, hub status and hovered chart markers were removed as likely flashing sources; genuine loading skeletons remain. Meetings attendance over time is now a searchable multi-select comparison with multiple coloured programme series and clickable points.
- Checks passed: `npm run test:unit` (158 tests), `npm run build`, and `git diff --check`. Built-in browser verification covered the shared home chart, project-styled tither comparison, outcome modal, labelled history controls, and the searchable two-series Meetings graph; the service setup modal appeared without the former eager attendance lookup. No backend, deployment or church-record write was performed.
- State: `codex/staging-accounts-and-verification` at `9abda19`; this refinement and substantial inherited work remain uncommitted and preserved.

### Development section — approved design and model-switch handoff (8 September 2026)

- User approved implementing the dedicated Development section and chose to save a handoff before switching to Terra to conserve usage. Implementation has not started; the previous profile radar/review changes remain in place.
- Continue from [Development implementation brief](development-section.md). It records the approved concept, searchable main/comparison person selectors, preserved filters, revised weeks/meetings calculations, programme breakdowns, monthly tithing, distinct collector/inviter outcomes, qualitative reviews, and persisted growth agreements.
- Important implementation requirements: scoped gathering-summary access, explicit collector attribution, honest register/data coverage, saved agreement review history, existing merge invariants, and frontend/backend/browser verification. The concept uses fictional records and is not the application implementation.
- This handoff changed documentation only. No deployment, data write, model switch, agent or new task was performed. State remains `codex/staging-accounts-and-verification` at `9abda19`, with extensive inherited uncommitted work preserved.

### Development section — initial implementation (8 September 2026; superseded by repair below)

- Added `/development`, a profile entry point, URL-preserved main/comparison selection, shared dates, scoped programmes, evidence using held known-register denominators, monthly recorded-tithe markers, the existing written leader-review workflow, and persisted server-attributed growth-agreement creation/progress/completion. The former large profile participation/review panel now links to this canonical workflow.
- Added explicit `collected_by_id` and indexed growth-agreement/review tables. The new purpose-specific Development summary returns only scoped-person evidence and minimal gathering metadata; ordinary gathering/attendance APIs remain restricted. Missing service register completion stays visibly unknown/excluded rather than becoming absence.
- Added distinct collector/inviter outcomes: brought requires a confirmed first Sunday-service attendance in the period and returned requires a later distinct Sunday-service attendance. The backend only supplies evidence within the caller's RLS scope; inaccessible evidence is never represented as absence.
- Checks passed: `npm run test:unit` (161), `npm run test:backend` (47), `npm run check:backend`, `npm run build`, and `git diff --check`. Tests cover held/known weekly denominators, tithing month boundaries/deduplication, first-visit/return attribution, agreement append history and access boundaries.
- Browser verification remains unavailable: two local browser-bridge attempts timed out/detached before an accessibility tree could be captured. This is an environment/tooling limitation, not a claimed visual pass. No deployment, live data write, seed, reset, or production action occurred.
- State: `codex/staging-accounts-and-verification` at `9abda19`; all work remains uncommitted alongside the preserved inherited tree.


### Development section — design and behaviour repair (8 September 2026)

- Rebuilt the initial Development page against the approved concept. Restored the four-axis comparison radar with Weeks/Meetings, exact counts, partial-data gaps, searchable person/comparison controls, styled period and category-specific programme pickers, monthly giving strips and shared-scale outreach bars. Preserved person, comparison, period and programme selections through URLs and history. Written reviews and growth agreements have separate views. In-page unsaved-draft confirmation keeps the person picker and draft consistent when navigation is cancelled.
- Corrected BST calendar boundaries, complete/partial monthly coverage, exact gathering-ID matching, duplicate attendance, unknown registers, programmes never attended, and earliest-known Sunday first/return visits. Summary reads use date indexes plus person-linked historical lookups and return minimal gathering projections. Confidential giving and agreement details remain restricted; limited outreach scope is labelled. No composite spiritual score or inferred role changes.
- Growth agreements support create, supporter/dates/notes, append-only progress reviews, completion/reopening and authored history. Failed saves keep drafts and retry keys; backend validates dates/scope and prevents duplicate retries. Added collector capture/correction in the outreach form and preserved collector, supporter, agreement and review links during person merges.
- Follow-up to the user's missing-tithing observation: existing demo attendance had no dated giving despite static tither flags. Added explicit fictional dated tithe examples in the demo generator, with consistent service aggregates and different monthly patterns. Verified Samuel 3/3 and Grace 2/3 for June–August, with clickable recorded dates. Live mode continues to require actual `gave_tithe` attendance entries; no live data was synthesized or changed.
- Checks passed: `npm run test:unit` (169 tests), `npm run test:backend` (50), `npm run check:backend`, `npm run build`, and `git diff --check`. Added coverage for month/date/event calculations, dated demo giving, scoped/minimal summary responses, confidential redaction, agreement retry/history/date validation, collector correction and merge invariants.
- Built-in browser verified desktop comparison, programme filtering without clearing other categories, custom periods, person search/no matches, comparison clearing, back navigation, agreement create/review/complete/reopening the person's saved history, written review saving, and both choices in the unsaved-draft dialog. At 390px, radar and agreement editor fit without horizontal overflow (382px document). Browser save checks used explicit in-memory demo; backend persistence/access was checked with Convex tests.
- Remaining release requirement: deploy matching schema/functions/frontend to the selected staging environment and verify authenticated reads/saves there. No deployment, production change or live church-record write occurred. Individual records and dates remain necessary for meaningful giving/outreach evidence; unconfirmed registers and unavailable history are labelled rather than counted as failure.
- State: `codex/staging-accounts-and-verification` at `9abda19`. Repair and extensive inherited changes remain uncommitted; unrelated work preserved.

### Development names and profile links — 8 September 2026

- Removed phone suffixes from ordinary person/comparison options. Only duplicate full names receive an explicit “Phone ending …” label, using case/whitespace-normalised name matching.
- Main name/avatar, comparison name, chart legend and person names in tithing/outreach/programme details now link to the corresponding profile, with visible link/focus styling. Search selectors continue selecting the Development subject; existing unsaved-draft navigation protection still applies to profile links.
- Checks: 169 frontend tests, production build and whitespace check passed. Built-in browser confirmed clean selector labels and main/comparison name clicks opening Samuel’s and Grace’s respective profiles.
- State: same `codex/staging-accounts-and-verification` branch, base `9abda19`, uncommitted alongside preserved inherited work. No backend or deployment changes.

### Development outreach demo counts — 8 September 2026

- User clarified that the outreach totals were staying at zero. Added eight explicitly fictional, dated outreach examples to the local demo generator, with separate collector/inviter links and linked first/return Sunday attendance. Totals are derived by the existing evidence calculation; no hardcoded chart totals or live fallback. Service attendance/guest totals and named lists include the added demo visits.
- Verified last-three-complete-month counts in the browser: Samuel 5 collected / 3 brought / 2 returned; Grace 3 / 2 / 1. Added a deterministic test covering attribution differences, period changes, linked attendance, consistent totals and duplicate-safe generation. All 170 frontend tests, build and whitespace checks pass.
- State remains uncommitted on `codex/staging-accounts-and-verification`, base `9abda19`, with inherited work preserved. No backend changes, deployment or live data writes.

### Analytics interaction cleanup — 8 September 2026

- Corrected Services’ Service mix so selecting a type no longer removes the card or traps the filter; its distribution remains based on the current date/search scope. Attendance trend and meeting-comparison tooltips now follow the hovered point and remain within their chart in normal and fullscreen views. Meeting comparison uses compact date labels, and the Development programme picker has bounded scrolling and resilient wrapping.
- The attendance matrix now accepts a typed Sunday count and gives its date columns a true minimum width with horizontal scrolling. Sidebar route preloading reduces the perceived wait when opening Meetings. Meetings filter copy now explains the primary choices and relegates the rest to optional refinements.
- Checks: `git diff --check` and `npm run build` pass. Built-in browser verification confirmed that selecting Special leaves the Service mix control visible; its two type totals remain correctly based on the whole in-scope period. Church-map marker, profile distance and travel time need a real church location plus a scoped configuration/data design; no location was invented for live data.
- State: `codex/staging-accounts-and-verification`, uncommitted alongside inherited work. No backend, deployment, production, or church-record write was made.

### Password sign-in and restricted leader access — 9 September 2026 (incomplete onboarding)

- Replaced forced Google login with Universal Login; confirmed email/password is already enabled in Auth0. Existing Google owner access is preserved. Added verified-email enforcement for password identities, giving-field read/write protection, self-assigned leader outreach creation, and restricted form/navigation/follow-up controls.
- Checks: 178 frontend tests, 56 backend tests, backend TypeScript, build, whitespace checks and the existing Playwright critical workflow pass. Built-in browser verified real email/password, sign-up and reset-password entry points on the already-approved local staging origin.
- Took and integrity-checked a file-inclusive staging snapshot, then deployed current integrated backend/schema to confirmed isolated `hidden-guineapig-648`. No production/frontend release, source-sheet change or record import. Original local frontend development URL restored after CLI configuration changes.
- Jayden created and verified his password identity and it was approved by the existing owner as an active staging owner; original owner retained. Approval rechecks now continue without reloading. Password entry into the protected app is verified by user confirmation, visible app navigation and a fresh Auth0 database-identity login. Remaining: restricted users and live permission/workflow checks, hosted release, import reconciliation and routine backups. Full details: [Password access handoff](password-access-handoff.md).
- State: `codex/staging-accounts-and-verification` at `9abda19`; uncommitted alongside preserved inherited work.

### Pastoral Care live navigation — 9 September 2026

- Fixed route initialization reading `contacts` from a null demo workspace in live mode, which left the previous page visible after clicking Pastoral Care. Live initialization now starts empty and loading; failed reads retain the visible retry state without demo records.
- Fixed session cleanup calling the nonexistent `ConvexClient.clearAuth()` API. Closing and discarding the subscription client now allows clean sign-out and a fresh client for the next session.
- Checks: 180 frontend tests, production build and whitespace check pass. Regression tests render the actual live route with a null demo workspace and failed backend read, and verify subscription cleanup/recreation. Browser logs confirmed both original exceptions; the updated browser correctly shows sign-in after session loss. Final authenticated browser navigation remains pending user sign-in; the login panel is open.
- State: `codex/staging-accounts-and-verification`, base `9abda19`, uncommitted alongside preserved inherited work. No backend deployment or church-record writes in this fix.

### Distinguishable sign-in accounts — 9 September 2026

- Manage access now displays a provider-derived Google / Email and password label, the current-account marker, email, and a plain role/confidential-access description. No provider subject is exposed in the managed-account projection. Missing legacy emails are labelled honestly; existing approval requests provide an email fallback, and new approvals persist their email.
- Checks: 57 backend tests, backend TypeScript, production build and whitespace check pass. Regression coverage checks same-name identities, current-account matching, historical email recovery and owner-only listing. Browser testing is left to Jayden as requested.
- Updated isolated staging `hidden-guineapig-648` with explicit env selection and restored the original `.env.local` afterward. No roles changed, accounts removed, or production release made. Uncommitted on `codex/staging-accounts-and-verification` alongside preserved inherited work.

### Follow-up subscription sign-in retention — 9 September 2026

- Code inspection identified live subscription token renewal as a likely sign-out trigger: forced Auth0 renewal failure immediately cleared an otherwise valid session. On forced-renewal failure, the app now permits only a same-session cached ID token with matching claims and more than 30 seconds remaining; otherwise sign-in is required. Late failures from a previous session cannot sign out a newer session.
- Added regression coverage for failed renewal with a valid cached token and expiry. Tests and browser verification intentionally not run, as Jayden requested to handle testing. This is a likely-cause repair pending his reproduction check, not a verified browser diagnosis.
- Frontend-only, uncommitted alongside inherited work on `codex/staging-accounts-and-verification`. No account or permission changes, backend deployment or production release.

### Shared church version and initial backups — 9 September 2026

- Confirmed the existing empty live destination `elated-bee-284`, installed matching sign-in settings and the current integrated backend, and published the frontend at https://my-church-tracker.vercel.app. Host returned READY. Provisioned the already approved password-owner identity; readback shows one owner, zero people/services. Practice data/accounts preserved; import remains held.
- Matched the host's Node.js 24 runtime, excluded private/operational artifacts from deployment, and added an explicitly targeted private file-inclusive backup command. Pre-change and post-setup local copies passed archive integrity checks; provider backup 1590445 completed with files, expiring 16 September. No church-record import or restore.
- Regular backups remain pending the user's free Mac-dependent versus paid provider choice. No paid upgrade or recurring schedule enabled. User browser/sign-in/workflow verification remains pending; no tests were run. Deployment-required build/schema processing and provider/readback checks completed.
- Full configuration, evidence, backup paths and remaining launch requirements: [shared launch handoff](shared-launch-handoff.md). State: `codex/staging-accounts-and-verification`, base `9abda19`; integrated inherited work and new setup changes remain uncommitted. No commit/push/reset/stash.

### Shared Google sign-in approval error — 9 September 2026

- User confirmed password sign-in works on the shared site; Google showed a generic failure. Live backend logs showed `access:me` rejecting an unapproved account with `ACCOUNT_NOT_APPROVED`. Production Convex preserves this reason in `error.data` while hiding the server message, so message-only checks missed the approval flow.
- Added shared structured access-error handling to session initialization and authenticated requests. Unapproved users retain the ability to request approval; email-verification and unauthenticated failures still remove access. No account grants or identities were changed.
- Added regression cases using masked production error messages and updated the session approval-request case. Tests/browser verification intentionally not run, per user preference. Production hosting build completed; deployment `dpl_3N7geonzY8QoWVWN4oyWtTAECgU7` is READY. User should refresh the permanent site and retry Google, then request approval and approve that identity through the existing password-owner account if desired.
- Frontend-only release; no backend deployment or church-record write. Branch `codex/staging-accounts-and-verification`, base `9abda19`; all inherited work and this repair remain uncommitted.

### Church import continuation — 9 September 2026 (awaiting import approval)

- Re-read the private review/source snapshot and refreshed the live Google Sheet through the connected Google Drive read path. Its four tabs and reviewed bounds remain present. The explicit destination is now the shared church version, `elated-bee-284`; practice remains `hidden-guineapig-648` and was not read or changed.
- Read-only live inventory: zero people, services, attendance, meetings, assignments and tasks; five existing programme definitions retained; two active owner accounts remain unlinked to people. No account-person link was inferred or changed.
- Added a persisted owner-controlled church settings model for Mustard Seed Chapel International Milton Keynes, the confirmed Castle House address, ACTS constituency and Jayden’s bacenta tracked scope, including verified links to retained programme records. It is not deployed or populated yet. Coordinates remain absent.
- Added import-safe schema and internal writer support: missing surname status without a synthetic surname; day/month birthdays and age bands without a guessed year; shared collector relationships distinct from inviter/CRM ownership; historical-note storage; source/batch/row provenance with repeat-safe source keys; Sunday present/absent/unknown registers; and explicit first-visit/recorded-return evidence. The writer does not search or merge people by name, create follow-up tasks, attach accounts, or read/write the source Sheet.
- Import execution remains held pending the user’s exact row decisions and explicit approval. Before any approved execution, take a new file-inclusive `elated-bee-284` backup with `scripts/backup-live.py`; routine backups remain undecided and no purchase, schedule or deployment was made.
- Checks: read-only Google Sheet metadata/ranges and explicit production-target inventory; `git diff --check` passed. Browser checks and all test/build/backend execution were intentionally left to Jayden. State: `codex/staging-accounts-and-verification`, base `9abda19`, uncommitted alongside the inherited working tree; no reset, stash, commit, deployment, source edit or church-record write.

Update: Jayden confirmed every reviewed source identity link except Dan↔Daniel and Cristina↔Christiana; those pairs remain distinct for later evidence-backed linking in the app. 24 May Frankie/Tegan is approved as present but left early; Frankie remains a source-only identity. Use Date added as first-contact date except the explicitly contradictory Levy row, which retains its historical note without an invented exact date. Profile-sheet people are members; outreach- and Sunday-only people are guests. Preserve source ChurchRole separately without access/leadership grants, and omit the malformed Jayden profile phone plus the marked wrong-number outreach phone from usable fields. Final exact plan and explicit import approval are still required before backup/import.

### Church import — applied shared-church batch (9 September 2026)

- After Jayden’s explicit approval, refreshed the reviewed source (fingerprint `fnv1a32:02567f33`) and created a new file-inclusive pre-import backup at `/Users/jaydenayeh/.codex/private/church-backups/elated-bee-284/20260909T162254Z-a93b6bad`. Routine backups remain unresolved; no purchase or schedule was enabled.
- Deployed the reviewed import/settings support to the confirmed shared deployment `elated-bee-284` and applied repeat-safe batch `n970zrg61yqxzcd5fr4r15bd9n8e2t8v`. Practice `hidden-guineapig-648` was not changed.
- Saved the confirmed church record and retained the existing Jayden bacenta programme link. Imported 95 people (12 members, 83 guests), 18 Sunday services and 361 explicit register states: 132 present, 128 absent and 101 unknown. This includes Frankie and Tegan on 24 May as present/left early. Services total 132 named attendances; no unnamed attendance was invented.
- Preserved 115 shared collector credits, 7 explicit first-visit records, 3 later recorded returns and 37 restricted historical notes (24 Sunday plus 13 outreach). The 75 non-conflicting outreach Date added values became contact dates; Levy's exact date remains unset. No inviter/follow-up ownership, account-person link, account change, name-only merge, or source-sheet edit was made. Dan and Cristina remain separate from Daniel and Christiana.
- Read-only post-import reconciliation and `git diff --check` passed. Browser checks and all test/build/backend test execution were intentionally left to Jayden. State: `codex/staging-accounts-and-verification`, base `9abda19`, with extensive inherited uncommitted work preserved; import/settings code and documentation remain uncommitted.
