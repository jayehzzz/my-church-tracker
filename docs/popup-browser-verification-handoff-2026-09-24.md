# Popup browser verification handoff — 24 September 2026

Batch A–G code is local and uncommitted in the **saved project directly** at `/Users/jaydenayeh/my-church-tracker`, branch `codex/practice-workflow`, inherited HEAD `d5cb782`. Use that same existing branch and working tree. A new default worktree from `main` omits this implementation. Do not reset, stash, clean or switch branches. No production release or Practice deployment is authorized by this handoff. The hosted Practice site is unchanged and does not yet contain these local frontend/backend changes; opening it must preserve its pinned database and test edits. Refresh Practice only on explicit request.

For initial browser verification, run the current source in local demo mode with deterministic fixtures. Clear any conflicting inherited live/rehearsal environment variables **for the process**, without editing `.env` files: `VITE_APP_ENV=development VITE_APP_MODE=demo VITE_APP_VARIANT='' VITE_CONVEX_URL='' npm run dev -- --host 127.0.0.1 --port <free-port>`. Inspect an existing server on 5197 before reusing it; it predates this task and may serve different source or mode. Use the built-in browser. `npm run test:e2e` uses the Task04 in-memory authenticated fixture on port 5194 for one critical workflow; it does not cover the new popup paths. Run it in this separate browser stage if feasible, then perform the additional interactive walkthroughs below. Do not deploy just to enable this task.

Check desktop and approximately 390px mobile, normal and fullscreen charts, mouse and keyboard activation, both A/B series, grouping and filter changes, Back/Close/Escape and focus return, scrolling/long lists, empty and unavailable/restricted detail. For each domain, record the selected figure, filters and exact source IDs/counts and walk **figure → source list → record → person → return** where a person exists. Confirm the originating tab, filters, list, scroll and focus return; a deleted/restricted row must not reappear from cached state. Test user/role/confidential changes and sign-out across a saved return frame. Capture route, selected measure, expected versus actual behavior and screenshot for any failure.

Domain checks:

- Dashboard and Services: Sunday total/average source services, latest-eight fallback date context, A/B comparison, members/guests/unknown/tithers, service detail and named/restricted/unnamed counts. Matrix cell and summary history must keep its selected Sunday period and status filter, including missed entries; Back returns to that history. Zero-service average says unavailable.
- Meetings and People: programme bar name and selected dates, Series A/B and grouping, meetings-held versus distinct people, linked attendee IDs without loaded names, event → person → return, exact meeting link, profile mixed service/meeting history with correct action label, directory comparison/map return.
- Evangelism and Follow-Up: contact cohort month versus later outcome date, zero-month average, inviter popup → recent contact → Back to inviter list and inviter profile → return popup, contact detail, worker historical evidence and attention task/person context, including due/overdue/unassigned/expected-Sunday differences.
- Pastoral Care and Development: care day/total/follow-up list → interaction → Back, read-only notes only when permitted; development participation, giving, outreach and two-person Series B evidence, unknown registers, partial months and permission changes.
- Reports and Memories: comparison Series B and monthly average through zero months to exact record, return without changing CSV selection, report scope/count arithmetic, album media position and exact service/meeting links.

The main integration paths live in `src/routes/{+page.svelte,services/+page.svelte,meetings/+page.svelte,people/+page.svelte,people/[id]/+page.svelte,evangelism/+page.svelte,pipeline/+page.svelte,visitation/+page.svelte,development/+page.svelte,reports/+page.svelte,memories/+page.svelte}`; shared behavior is in `src/lib/components/drilldown/`. Automated gates for Batch G are recorded in `docs/implementation/README.md`. The local frontend and the extended Convex responses must eventually be released together for live worker/development evidence. That release and any hosted Practice update are separate decisions. Recommended verification model: GPT-6 Luna/Low if available, escalating only for a concrete calculation or access issue. No new user task was created.
